# Environment Configuration
NODE_ENV=development
PORT=3000

# Database Configuration (Optional)
# The system uses SQLite by default for local development
# For production, you can use PostgreSQL or Supabase
# DATABASE_URL=postgresql://user:password@host:port/database

# Default Admin Credentials (Change these in production!)
# Username: admin
# Password: admin123

# SSL Certificate Generation (Auto-generated if not present)
# The system will automatically create self-signed certificates for HTTPS